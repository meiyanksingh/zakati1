<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mks.zakati">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SelectTypeActivity" />
        <activity android:name=".activities.SignupActivity" />
        <activity android:name=".activities.TermsActivity" />
        <activity android:name=".activities.NameActivity" />
        <activity
            android:name=".activities.EmailndNameActivity"
            android:label="@string/title_activity_emailnd_name"
            android:theme="@style/AppTheme" />
        <activity android:name=".activities.CreatePassActivity" />
        <activity android:name=".activities.YourDetailActivity" />
        <activity android:name=".activities.AboutYourSelfActivity" />
        <activity android:name=".activities.SetProfilePicActivity" />
        <activity android:name=".activities.LoginActivity"></activity>
    </application>

</manifest>